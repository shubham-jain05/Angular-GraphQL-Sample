(function (window) {
    window.__env = window.__env || {};
    window.__env.PRODUCTION = "${PRODUCTION}";
    window.__env.ENVIRONMENT_TYPE = "${ENVIRONMENT_TYPE}";
    window.__env.API_URL = "${API_URL}";
    window.__env.API_URL_MASK = "${API_URL_MASK}";
  })(this);
  